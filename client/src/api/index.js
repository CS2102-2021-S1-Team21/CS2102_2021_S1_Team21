import petOwners from './petOwners';
import leaves from './leaves';

/* ====================================
 * Index of all API request routes
 * ====================================
 */

const api = {
  petOwners,
  leaves,
  // pets,
  // caretakers,
};

export default api;
